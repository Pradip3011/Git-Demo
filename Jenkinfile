pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'Build triggered'
                git 'https://github.com/Pradip3011/Git-Demo.git'
            }
        }
    }
}
